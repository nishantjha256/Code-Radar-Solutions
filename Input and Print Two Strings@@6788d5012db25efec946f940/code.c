#include <stdio.h>
int main(){
    char str1[100];
    char str2[100];
    printf("Enter two space-separated strings:");
    scanf("%s %s",str1, str2);
    printf("You entered: %s and %s\n,, str1, str2");
    return 0;
    
}